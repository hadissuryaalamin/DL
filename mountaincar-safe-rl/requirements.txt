# Pinned to the versions used to produce the final results (Python 3.14).
# For older interpreters (< 3.14) the lower bounds below are known to work.
gymnasium==1.3.0
numpy==2.4.6
torch==2.12.0
scipy==1.17.